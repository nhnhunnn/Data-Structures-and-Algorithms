#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll mod = 1e9 + 7;

int main(){
	int t; cin >> t;
	while(t--){
		int n, m, k; cin >> n >> m >> k;
		int a[n + 1], b[m + 1];
		for(int i = 1; i <= n; i ++) cin >> a[i];
		for(int i = 1; i <= m; i ++) cin >> b[i];
		ll res = 0;
		
	}
	return 0;
}