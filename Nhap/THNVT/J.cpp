#include <bits/stdc++.h>
using namespace std;
typedef pair<int, int> p;
p st, en;
vector <p> v;
int vs[100005][2];
void bfs(){
	
}
int main(){
	int n; cin >> n;
	cin >> st.first >> st.second >> en.first >> en.second
	for(int i = 0; i < n; ++i){
		int x, y; cin >> x >> y;
		v.push_back({x, y});
	}
}
